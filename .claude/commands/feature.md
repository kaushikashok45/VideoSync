---
description: Run the feature pipeline. Stage 0 interrogates the request interactively, then spawns the cold stages.
argument-hint: "<the feature request, in the requester's own words>"
---

Read and follow the canonical command in `.agents/commands/feature.md`. The request supplied by this command is `$ARGUMENTS`.
